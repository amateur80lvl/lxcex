#!/bin/sh -e

# there seems to be no easy way to know HOST_ variables and
# revoke permissions
#setfacl -x g:$1 "${HOST_XDG_RUNTIME_DIR}"
#setfacl -x g:$1 "${HOST_XDG_RUNTIME_DIR}/${HOST_WAYLAND_DISPLAY}"
